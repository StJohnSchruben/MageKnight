<UserControl x:Class="MKView.Views.MageKnightBattleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MKView.Views"
             mc:Ignorable="d" 
             Height="800" Width="800" MouseDoubleClick="UserControl_MouseDoubleClick">
    <UserControl.Resources>
        <local:BoolToVisibility x:Key="BoolToVisibility" />
    </UserControl.Resources>
    <Grid>
        <local:ArcControl Visibility="{Binding IsSelected, Converter={StaticResource BoolToVisibility}}"/>
        <local:Dial DataContext="{Binding Dial}"/>
    </Grid>
</UserControl>
